services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=products
    volumes:
      - products_pg_data:/bitnami/postgresql

volumes:
  products_pg_data: {}